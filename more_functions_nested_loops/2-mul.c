#include "main.h"

/**
 * mul -	multiplies two integers
 *
 * @a:		le 1er nombre a multiplier
 * @b:		le 2nd nombre a multiplier
 *
 * Return:	le resultat de la multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
